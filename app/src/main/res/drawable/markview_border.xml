<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 实心 -->
    <solid android:color="@color/marker_bg" />
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@android:color/white" />
    <!-- 圆角 -->
    <corners android:radius="1dp" />
    <!-- 边距 -->
    <padding
        android:bottom="@dimen/markview_space"
        android:left="@dimen/markview_space"
        android:right="@dimen/markview_space"
        android:top="@dimen/markview_space" />
    <!-- 渐变 -->
    <!--<gradient-->
        <!--android:angle="270"-->
        <!--android:endColor="#FFFF782"-->
        <!--android:startColor="#13C7AF" />-->

</shape>